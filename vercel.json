{
  "version": 2,
  "buildCommand": "chmod +x install.sh && ./install.sh && ./flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "echo 'Using custom install script'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}